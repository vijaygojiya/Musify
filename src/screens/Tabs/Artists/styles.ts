import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  columnWrapperStyle: {
    gap: 8,
  },
  flContainerStyle: {
    gap: 30,
    paddingVertical: 10,
    marginHorizontal: 19,
  },
});

export default styles;
