import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  columnWrapperStyle: {
    gap: 20,
  },
  flContainerStyle: {
    gap: 30,
    paddingVertical: 10,
    marginHorizontal: 20,
  },
});

export default styles;
